# Environment Variables for Production

# MongoDB Connection String
# Get this from MongoDB Atlas (cloud database)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/music_streaming_db?retryWrites=true&w=majority

# Session Secret (Random string for securing sessions)
# Render will auto-generate this, or create your own random string
SESSION_SECRET=your_super_secret_random_string_here

# Node Environment
NODE_ENV=production

# Port (Render sets this automatically)
PORT=3000
